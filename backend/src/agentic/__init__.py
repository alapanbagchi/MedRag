"""Agentic v3 - the run engine: pipeline, domain state, events, worker pipelines.

The agent actors that this engine orchestrates live in src.agents; the
non-LLM capabilities they use live in src.tools.
"""
