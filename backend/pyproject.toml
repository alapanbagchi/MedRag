[project]
name = "medrag"
version = "0.1.0"
description = "Structure-aware RAG pipeline for PMC/JATS biomedical articles"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "boto3",
  "docling",
  "faiss-cpu",
  "httpx",
  "logfire",
  "deepagents>=0.7.12",
  "langchain-openai",
  "langchain-text-splitters",
  "langgraph>=1.2.11",
  "lxml",
  "numpy",
  "pandas",
  "pgvector",
  "psycopg2-binary",
  "pyarrow",
  "pydantic",
  "pydantic-ai[logfire]",
  "python-dotenv",
  "requests",
  "scipy",
  "torch",
  "tqdm",
  "transformers",
  "fastapi>=0.141.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
  "core",
  "src",
  "src.agents",
  "src.agentic",
  "src.tools",
  "src.chunking",
  "src.ingestion",
  "src.lib",
  "src.llm",
  "src.memory",
  "src.processing",
  "src.prompts",
  "src.prompts.agents",
  "src.retrieval",
  "src.umls",
  "src.x_deepagents",
  "src.x_deepagents.agents",
  "src.x_deepagents.tools",
]

[tool.setuptools.package-data]
"src.prompts" = ["*.txt"]
"src.prompts.agents" = ["*.txt"]

[dependency-groups]
dev = [
  "pytest",
  "pytest-asyncio",
]
